TO RUN UNICORN - uvicorn main:app --reload

Create a virtual environment:

python -m venv venv

Activate it.

Windows
venv\Scripts\activate

Mac/Linux
source venv/bin/activate

Install:

pip install fastapi uvicorn requests